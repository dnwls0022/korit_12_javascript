# 쇼핑리스트

todolist 작성한 부분을 github에 올릴건데 중요한 부분은 ai 사용하고 나서 어떻게 붙여넣을지 입니다.

todolist.html을 기준으로 보냈기 때문에0 .css와 .js가 기존 버전으로 되어있어서 html만 BEM 버전으로 바꾼다고 하더라도 실제로는 CSS가 적용되지 않습니다.

특정 방법을 사용했을 때 여러분이 작성한거 다 날리고 덮어쓸 수도 있겠지만 남겨놓고 CSS 버전만 다양하게 갈아끼우고 싶다면 남겨두는 방법도 필요합니다.

레퍼런스를 아마 AI로 만들 확률이 높기 때문에 기존 문서에서 적용하기 위해서는 link 태그 사용 후에 href를 잘 적용시켜

- input 창에 가격(price)추가 v

-js 부분수정... renderTodo()부분에일단 html태그가추가 span

- html태그에있는 todolist기준의 bem을 전부 shoppinglist기준으로 bem으로 수정할것 ai사용.....

1/29
로그인 창 
회원가입 창 
test


# Fetch Api
- 정의
네트워크 통신을 포함한 리소스 취득을 위한 인터페이스가정의 되어있는 모음
-resource : DB에 저장되어있는값
- crud
creat  : 데이터 생성
read   : 데이터에 저장되어있는것을 일거옴
update : 데이터 갱신
delete : 데이터 삭제

- localStorage todo / shopping item 을 추가하고 알아오고 삭제

- jsonplaceholder를 활용하여 crud 학습 ----> Fetch api

https://jsonplaceholder.typicode.com/
을 기준
1. /posts
2. /comments
3. /albums
4. /photos
5. /users
6. /todos

임의의 json데이터를 활용하여 프론트엔드에 표현하는것이가능

## promise
자료형의일종

```HTML
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body bgcolor="5f5f5f">
  <h1></h1>
  <script>
    //형식에주목
    // crud의 r   ..... get/ read : 읽어오기 / id가 1인데이터조회
    //fetch 가져오다 
    fetch('https://jsonplaceholder.typicode.com/posts/1')
    .then(responce => responce.json())// 결과값(responce)을 임의로 .json이란 메서드 ........json결과값나옴
    .then((json) => console.log(json.body)); // id 다른것들과 구분되는 유일한고유값 
  
  //Post / create : 데이터생성
      fetch('https://jsonplaceholder.typicode.com/posts',{
          method: "POST",
          body : JSON.stringify({
          title: 'foo',
          body: 'bar',
          userId: 1,
          }),
          headers: {
            "content-type" : " application/json; charset=UTF-8 "
          
          }
      }) 
      .then(responce =>  responce.json())
      .then(json=> console.log(json));
      // Put / Update : 데이터수정
      //fetch의 결과 promise 비동기적실행
      fetch('https://jsonplaceholder.typicode.com/posts/1',{
        method:'PUT',
        body: JSON.stringify({
          id:1,
          title:"다음에 promise",
          body:'자습',
          userId:1,
        }),
        headers:{
          'Content-type': 'application/json; charset=UTF-8 ',
        },
      })
      .then(responce =>  responce.json())
      .then(json=> console.log(json));

      //Delete - 데이터삭제
      fetch('https://jsonplaceholder.typicode.com/posts/1',{
        method:'DELETE',

      });
      fetch('https://jsonplaceholder.typicode.com/posts/')
      .then(responce =>  responce.json())
      .then(json=> console.log(json));
      // 삭제후엔 todolist기준으로 renderTodo를 새로해야함
      // C/U/D 뒤엔 새로 R을 해야한다.
      // .then(responce =>  responce.json())
      // .then(json=> console.log(json)); 고정
  </script>
</body>
</html>
```

axios 라이브러리 도입하면 좀 더 간단한 방법으로 코드를 작성할수있다. 

# promise & Async/ Await(동기/비동기)

- 자바스크립트상의 비동기처리에 사용되는 객체 비동기처리란 특정코드의 실행이 완료될 때까지 기다리지 않고 코드를 실행할수 있도록 하는 방식
- 원래 웹은 요청에 대한 반응이 순차적으로 이루어지고 먼저실행된코드가 완료되어야 다음 코드를 실행하는 순서를 가지고있었는데, 개선 이후에 요청을 보낸 후 요청에 대한 응답이 돌아오는 것과 관계 없이 다음 코드를 실행시킬수있다.

- promise는 js에서 브라우저에 제공하는 비동기함수인 web api 를 실행할 때 실행한 코드가 완료될때까지 대기하지않고 바로 다음 코드를 실행할수있도록해주고 비동기함수의 실행이 완료되면 .then함수를 통해 그 결과에 대한 코드를 실행

 - *비동기적이지만 동기적으로 쓸수있게끔하는 방식의라이브러리는...

 ## Async /Await 
 - promise와 동일한 목적으로 사용한다. Async비동기 실행을 의미하고 Await는 비동기 코드를 실행하는데 결과가 올 때까지 기다렸다가 쓴다는의미... 
 02_asyncawait.js
 ```JS
 const { forwardRef } = require("react");

function myfunction1(){
  fetch('https://jsonplaceholder.typicode.com/posts/1')
    .then(responce =>  responce.json())
    .then(json=> console.log(json));

}

myfunction2();
//get요청을 해서 

/**
 * 서버로 요청을 보내고 응답을 보낸후 응답받은 결과를 바탕으로 다시서버로 요청을보낸다.
 */

function myfunction2(){
  fetch('https://jsonplaceholder.typicode.com/posts/1')
      .then(responce =>  responce.json())
      .then(json=> console.log(json));

    
        
}

// 결과적으로 get 요청이후에 결과값을 가지고 put요청을 새로 한 후에 put의 결과를 확인할수있도록 get요청을 다시 날린형태임 가독성저하
// async await

async function myfunction3(){
  const res1 = await fetch('https://jsonplaceholder.typicode.com/posts/2');
  const res1JSON = await res1.json();
  console.log(res1JSON);
  // get요청부분에 해당한다 async / await를 빼면 일반적인 자바스크립트처럼 함수 실행 결과값을 변수에담는것처럼 보임


const res2 = await  fetch('https://jsonplaceholder.typicode.com/posts/2',{
  method : 'PUT',
  body: JSON.stringify({
    id:2,
    title:'나', 
    body:'응',
    userid:1,
  }),
  headers:{
          'Content-type': 'application/json; charset=UTF-8 ',
        },
});
const res2JSON = await res2.json();
console.log(res2JSON);

}
myfunction3();

```
## DOM이벤트
- DOM(Document Objebt Model) - > document.write()
- 웹 페이지를 이용하면 웹 화면과 사용자사이에서 많은 상호작용이있다. 버튼 클릭,마우스 스크롤, 검색 키워드를 입력하는 등의 행동이해당된다. 사용자가 웹페이지이용하묜서 하는 행위 event

1. click이벤트 : onclick
사용자가 웹 페이지내에서 마우스 왼쪽버튼클릭시 발생하는 이벤트 웹 화면에서 일어나는 모든 클릭이벤트를 감시할 필요는 없지만 '검색' '저장' 처럼 사용자가 클릭을 하고나면 연관된 기능을 처리해야하는 클릭이베트에 대해서는 js에서 함수를 정의하여 확인할필요가있다

- button()태그 요소 클릭 시 이벤트가 일어나는지 감시하고 클릭이벤트가 일어나는 순간 특정 함수를 실행시키려면 button태그에 onclick속성을 사용해서 처리가능 button태그에 id값넣고 거기에서 클릭이벤트를 addEventlistner()통해처리함


```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body bgcolor="5f5f5f">
  <button onclick="myFunction();">클릭</button>
  <button id="btn1">클릭</button>
  <button id="btn2">클릭</button>
  <script>
    function myFunction1(){
      console.log('onclick이벤트실행');
    }
    function myFunction2(){
      console.log('event리스너사용');
    }
    document.getElementById('btn1').addEventListener
    ('click',myFunction2);
    document.getElementById('btn2').addEventListener
    ('click',function(){
      console.log('콜백함수실행');
    });
    document.getElementById('btn3').addEventListener
    ('click', () => console.log(화살표함수실행));
    
  </script>
</body>
</html>
```
2. change 이벤트 : onchange
- 값이 변경되었을때 발생하는이벤트 대표적으로 change이벤트가 발생하는 dom요소는 '<select>',<input type= 'checkbox'>, <input type = 'radio'>태그이다. select는 여러개의 옵션에서 하나 이상의 옵션을 선택할 때 사용하는 dom요소로 사용자가 옵션을 바꿀 때 마다 웹에서는 change이벤트가 발생 
js에서 이벤트를 감시하여 특정함수를 실행하는 방식을 택함 onchange속성활용

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body bgcolor="5f5f5f">
  <select name="select1" onchange="dsChange();">
    <option value="KO">한국</option>
    <option value="CN">중국</option>
    <option value="JP">일본</option>
  </select>
  <script>
    function doChange(){
      let selectedValue = document.getElementById
      ('select').value;
      console.log(selectedValue);
    }
  </script>
</body>
</html>
```

3. key이벤트(onkeydown, onkeypress,onkeyup)
- key이벤트는 사용자가 입력창에서 키보드를 입력할때 발생 . 사용자가 키를 입력하면 keydown,keypress,keyup순으로 발생... 
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body bgcolor="5f5f5f">
  <input type="text"
        onkeypress="myFunction(Event);"
        onkeydown="myFunction(event);"
        onkeyup="myFunction(event);"
  >
  <script>
    function myFunction(e){
      console.log(e.type);
      console.log(e.target);
      console.log(e.target.value);
    }
  </script>
  
</body>
</html>
```
input type text 라면 e.target.value는 꼭기억...... input창에서 입력받은데이터가 db에 있는 id와 일치하는지 비밀번호와일치하는지체크
///////////////////////////////////////////////////////////////////

회원가입(sign up) - 아이디(id)비밀(password)
로그인(log in)-  이름(name)

todolist 